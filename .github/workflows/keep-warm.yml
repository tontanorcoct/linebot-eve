name: Keep Vercel Warm

# รันทุก 5 นาที
on:
  schedule:
    - cron:  '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-warm endpoint
        run: |
          curl -s https://linebot-dnij5466g-tontanorcocts-projects.vercel.app/api/keep-warm || echo "Ping failed"
